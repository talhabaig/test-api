const functions = require("firebase-functions");
const nodemailer = require("nodemailer");
const app = require("./routes/firebase-config");



exports.sendMail = functions.https.onRequest((req, res) => {
  
});
